{
  "name": "keycastjs",
  "productName": "KeyCastJs",
  "main": "src/main.js",
  "scripts": {
    "install": "electron-rebuild -w ref && electron-rebuild -w ffi && electron-packager . --platform=darwin --arch=x64 --version=1.3.5 --asar"
  },
  "dependencies": {
    "ffi": "node-ffi/node-ffi#gh-241",
    "nodobjc": "^2.1.0"
  },
  "devDependencies": {
    "electron-packager": "^7.0.3",
    "electron-prebuilt": "~1.3.5",
    "electron-rebuild": "^1.0.2"
  }
}
